{
  "name": "scheduler",
  "version": "1.0.0",
  "description": "A web application that helps you manage your tasks",
  "main": "",
  "scripts": {
    "install-frontend": "cd frontend && npm install && npm run build",
    "install-backend": "cd backend && npm install",
    "install-dependencies": "npm run install-frontend && npm run install-backend",
    "deploy": "pm2 deploy ecosystem.config.js production",
    "setup-remote-directories": "pm2 deploy ecosystem.config.js production setup"
  },
  "author": "Paul Michea",
  "license": "ISC",
  "dependencies": {}
}
