<form method="post" action="{{ path('activity_info_delete', {'id': activity_info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity_info.id) }}">
    <button class="btn">Delete</button>
</form>
