{% extends 'base.html.twig' %}

{% block title %}ActivityInfo{% endblock %}

{% block body %}
    <h1>ActivityInfo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activity_info.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ activity_info.description }}</td>
            </tr>
            <tr>
                <th>Priority</th>
                <td>{{ activity_info.priority }}</td>
            </tr>
            <tr>
                <th>Importance</th>
                <td>{{ activity_info.importance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('activity_info_index') }}">back to list</a>

    <a href="{{ path('activity_info_edit', {'id': activity_info.id}) }}">edit</a>

    {{ include('activity_info/_delete_form.html.twig') }}
{% endblock %}
